import { Lands } from '@/enums/lands';
import type { MapMarker } from '@/types';

const data: MapMarker = {
  name: Lands.NEYWYLL,
  coords: [560, 230],
  description: 'The realm of the elves',
  link: '/lands/hetheskein',
};

export default data;
