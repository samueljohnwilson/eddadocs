import { Lands } from '@/enums/lands';
import type { MapMarker } from '@/types';

const data: MapMarker = {
  name: Lands.GARRAD,
  coords: [1000, 1200],
  description: 'The realm of the horse lords',
  link: '/lands/garrad',
};

export default data;
