import { Lands } from '@/enums/lands';
import type { MapMarker } from '@/types';

const data: MapMarker = {
  name: Lands.ALBIA,
  coords: [735, 1500],
  description: 'The realm of the high elves',
  link: '/lands/albia',
};

export default data;
