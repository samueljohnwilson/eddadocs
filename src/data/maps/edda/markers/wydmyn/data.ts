import { Lands } from '@/enums/lands';
import type { MapMarker } from '@/types';

const data: MapMarker = {
  name: Lands.WYDMYN,
  coords: [535, 625],
  description: 'The realm of the half elvees',
  link: '/lands/wydmyn',
};

export default data;
