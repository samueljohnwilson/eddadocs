import { Lands } from '@/enums/lands';
import type { MapMarker } from '@/types';

const data: MapMarker = {
  name: Lands.RAJAPUN,
  coords: [185, 1330],
  description: 'The realm of the tropical dwarves',
  link: '/lands/rajapun',
};

export default data;
