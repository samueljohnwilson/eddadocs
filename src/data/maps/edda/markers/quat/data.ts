import { Lands } from '@/enums/lands';
import type { MapMarker } from '@/types';

const data: MapMarker = {
  name: Lands.QUAT,
  coords: [1250, 1300],
  description: 'The realm of the hunters',
  link: '/lands/quat',
};

export default data;
