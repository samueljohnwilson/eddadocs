import { Lands } from '@/enums/lands';
import type { MapMarker } from '@/types';

const data: MapMarker = {
  name: Lands.ITHRA,
  coords: [450, 1240],
  description: 'The realm of the mages',
  link: '/lands/ithra',
};

export default data;
